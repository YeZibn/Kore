- 采用模块化包结构：`kore` 根目录下按功能划分为 `api`（接口）、`llm`（模型工厂）、`runtime`（核心逻辑）、`memory`（记忆存储）等子模块。
- 依赖管理通过 `pyproject.toml` 声明，集成 `fastapi` 作为 Web 框架，`pydantic-settings` 处理配置，并支持 `openai`、`anthropic` 等多提供商 LLM。
- 配置体系基于环境变量（参考 `.env.example`），通过 `KORE_` 前缀统一管理 API 密钥及模型参数。