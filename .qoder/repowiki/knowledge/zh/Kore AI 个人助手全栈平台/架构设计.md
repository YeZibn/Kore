- **前后端分离架构**：采用 Tauri (React/TS) 作为前端交互层，FastAPI (Python) 作为后端逻辑层，两者通过 HTTP/SSE 协议通信。
- **Sidecar 模式集成**：Tauri 负责管理 Python 后端进程的生命周期（启动/停止），实现桌面应用的无缝体验。
- **模块化后端设计**：后端内部解耦为 AgentCore、LLM 抽象层、工具系统、记忆系统及知识库等独立模块，支持按需替换与扩展。
- **本地化数据存储**：统一使用 SQLite、ChromaDB 及本地文件系统处理持久化数据，确保零外部依赖的单机部署能力。