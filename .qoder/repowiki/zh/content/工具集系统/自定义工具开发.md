# 自定义工具开发

<cite>
**本文档引用的文件**
- [pyproject.toml](file://backend/pyproject.toml)
- [router.py](file://backend/kore/api/router.py)
- [base.py](file://backend/kore/llm/base.py)
- [factory.py](file://backend/kore/llm/factory.py)
- [agent_core.py](file://backend/kore/runtime/agent_core.py)
- [models.py](file://backend/kore/runtime/models.py)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖分析](#依赖分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介

Kore 是一个个人 AI 助手与智能体运行时框架，旨在为开发者提供构建和部署智能体应用的完整解决方案。本指南专注于自定义工具开发，帮助开发者理解如何在 Kore 框架中创建、实现和管理自定义工具。

Kore 框架采用模块化设计，支持多种 LLM（大语言模型）提供商，具备强大的工具系统，允许智能体执行外部操作。工具是 Kore 智能体能力的核心扩展点，通过工具可以实现网络请求、文件操作、计算等功能。

## 项目结构

基于当前代码库分析，Kore 框架的主要目录结构如下：

```mermaid
graph TB
subgraph "后端核心"
KORE[kore/]
API[api/]
LLM[llm/]
RUNTIME[runtime/]
SKILLS[skills/]
SOLVER[solver/]
TOOLS[tools/]
CHANNELS[channels/]
KNOWLEDGE[knowledge/]
MCP[mcp/]
MEMORY[memory/]
PROMPTING[prompting/]
STORAGE[storage/]
TRACING[tracing/]
end
subgraph "配置"
PYPROJECT[pyproject.toml]
end
KORE --> API
KORE --> LLM
KORE --> RUNTIME
KORE --> SKILLS
KORE --> SOLVER
KORE --> TOOLS
KORE --> CHANNELS
KORE --> KNOWLEDGE
KORE --> MCP
KORE --> MEMORY
KORE --> PROMPTING
KORE --> STORAGE
KORE --> TRACING
PYPROJECT --> KORE
```

**图表来源**
- [pyproject.toml:1-34](file://backend/pyproject.toml#L1-L34)

**章节来源**
- [pyproject.toml:1-34](file://backend/pyproject.toml#L1-L34)

## 核心组件

### LLM 基础架构

Kore 的 LLM 系统采用工厂模式设计，提供了灵活的模型选择和管理机制：

```mermaid
classDiagram
class LLMBase {
<<abstract>>
+model_name : str
+model_config : dict
+invoke(prompt) Any
+stream(prompt) Generator
+get_model_info() dict
}
class LLMFactory {
+models : dict
+register(model_name, model_class)
+create(model_name, config) LLMBase
+list_available_models() list
}
class OpenAIModel {
+client : OpenAI
+invoke(prompt) Any
+stream(prompt) Generator
}
class AnthropicModel {
+client : Anthropic
+invoke(prompt) Any
+stream(prompt) Generator
}
LLMBase <|-- OpenAIModel
LLMBase <|-- AnthropicModel
LLMFactory --> LLMBase : "创建实例"
LLMFactory --> OpenAIModel : "注册"
LLMFactory --> AnthropicModel : "注册"
```

**图表来源**
- [base.py](file://backend/kore/llm/base.py)
- [factory.py](file://backend/kore/llm/factory.py)

### 运行时核心

智能体运行时负责协调各个组件的工作：

```mermaid
classDiagram
class AgentRuntime {
+agent_core : AgentCore
+llm_manager : LLMFactory
+tool_registry : dict
+memory_manager : MemoryManager
+execute_task(task) TaskResult
+register_tool(tool_name, tool_class)
+process_query(query) AgentResponse
}
class AgentCore {
+current_state : AgentState
+memory : AgentMemory
+capabilities : list
+process_input(input_data) ProcessedData
+update_state(new_state) void
+get_capabilities() list
}
class ToolRegistry {
+tools : dict
+registered_tools : set
+register_tool(name, tool_class)
+get_tool(name) ToolBase
+list_tools() list
}
AgentRuntime --> AgentCore : "管理"
AgentRuntime --> ToolRegistry : "维护"
AgentRuntime --> LLMFactory : "使用"
AgentCore --> MemoryManager : "访问"
```

**图表来源**
- [agent_core.py](file://backend/kore/runtime/agent_core.py)
- [models.py](file://backend/kore/runtime/models.py)

**章节来源**
- [base.py](file://backend/kore/llm/base.py)
- [factory.py](file://backend/kore/llm/factory.py)
- [agent_core.py](file://backend/kore/runtime/agent_core.py)
- [models.py](file://backend/kore/runtime/models.py)

## 架构概览

Kore 框架的整体架构采用分层设计，确保了良好的可扩展性和可维护性：

```mermaid
graph TB
subgraph "用户界面层"
WEB[Web UI]
CLI[命令行界面]
API[REST API]
end
subgraph "业务逻辑层"
ROUTER[路由处理器]
AGENT_RUNTIME[智能体运行时]
TOOL_MANAGER[工具管理器]
end
subgraph "数据访问层"
MEMORY[内存管理]
STORAGE[存储系统]
DATABASE[(数据库)]
end
subgraph "外部服务层"
LLM_SERVICES[LLM 服务]
FILE_SYSTEM[文件系统]
NETWORK[网络服务]
end
WEB --> ROUTER
CLI --> ROUTER
API --> ROUTER
ROUTER --> AGENT_RUNTIME
AGENT_RUNTIME --> TOOL_MANAGER
TOOL_MANAGER --> MEMORY
TOOL_MANAGER --> STORAGE
MEMORY --> DATABASE
STORAGE --> DATABASE
AGENT_RUNTIME --> LLM_SERVICES
TOOL_MANAGER --> FILE_SYSTEM
TOOL_MANAGER --> NETWORK
```

**图表来源**
- [router.py](file://backend/kore/api/router.py)
- [agent_core.py](file://backend/kore/runtime/agent_core.py)

## 详细组件分析

### 工具系统架构

虽然当前代码库中没有看到具体的工具实现文件，但基于框架的设计模式，可以推断出工具系统的架构：

```mermaid
classDiagram
class ToolBase {
<<abstract>>
+name : str
+description : str
+parameters : dict
+execute(context) ToolResult
+validate_parameters(params) bool
+get_signature() dict
}
class NetworkTool {
+session : httpx.AsyncClient
+timeout : int
+execute(context) ToolResult
+fetch_data(url, params) dict
+post_data(url, data) dict
}
class FileTool {
+base_path : Path
+execute(context) ToolResult
+read_file(path) str
+write_file(path, content) bool
+list_directory(path) list
}
class CalculatorTool {
+execute(context) ToolResult
+add(a, b) float
+multiply(a, b) float
+calculate(expression) Any
}
class ToolRegistry {
+tools : dict
+metadata : dict
+register_tool(name, tool_class, metadata)
+get_tool(name) ToolBase
+list_available_tools() list
+validate_tool_usage(tool_name, context) bool
}
ToolBase <|-- NetworkTool
ToolBase <|-- FileTool
ToolBase <|-- CalculatorTool
ToolRegistry --> ToolBase : "管理"
```

**图表来源**
- [agent_core.py](file://backend/kore/runtime/agent_core.py)

### API 路由系统

Kore 使用 FastAPI 构建 REST API，提供统一的接口访问：

```mermaid
sequenceDiagram
participant Client as "客户端"
participant Router as "路由处理器"
participant Agent as "智能体运行时"
participant Tool as "工具执行器"
participant LLM as "LLM 服务"
Client->>Router : POST /api/agents/{agent_id}/tasks
Router->>Router : 验证请求参数
Router->>Agent : 创建任务
Agent->>Agent : 解析任务上下文
Agent->>Tool : 执行工具调用
Tool->>Tool : 验证工具参数
Tool->>LLM : 获取响应
LLM-->>Tool : 返回结果
Tool-->>Agent : 工具执行结果
Agent->>Agent : 处理结果
Agent-->>Router : 最终响应
Router-->>Client : JSON 响应
Note over Client,LLM : 异步处理流程
```

**图表来源**
- [router.py](file://backend/kore/api/router.py)

**章节来源**
- [router.py](file://backend/kore/api/router.py)

### LLM 工厂模式

Kore 的 LLM 系统采用工厂模式，支持多种模型提供商：

```mermaid
flowchart TD
START([开始]) --> CHECK_MODEL["检查模型名称"]
CHECK_MODEL --> MODEL_EXISTS{"模型已注册?"}
MODEL_EXISTS --> |是| CREATE_INSTANCE["创建模型实例"]
MODEL_EXISTS --> |否| REGISTER_MODEL["注册新模型"]
REGISTER_MODEL --> CREATE_INSTANCE
CREATE_INSTANCE --> INIT_CLIENT["初始化客户端"]
INIT_CLIENT --> CONFIGURE_PARAMS["配置模型参数"]
CONFIGURE_PARAMS --> VALIDATE_CONFIG["验证配置"]
VALIDATE_CONFIG --> CONFIG_VALID{"配置有效?"}
CONFIG_VALID --> |是| RETURN_MODEL["返回模型实例"]
CONFIG_VALID --> |否| HANDLE_ERROR["处理配置错误"]
HANDLE_ERROR --> ERROR_HANDLER["错误处理流程"]
ERROR_HANDLER --> END([结束])
RETURN_MODEL --> END
```

**图表来源**
- [factory.py](file://backend/kore/llm/factory.py)

**章节来源**
- [factory.py](file://backend/kore/llm/factory.py)

## 依赖分析

### 核心依赖关系

Kore 框架的依赖关系体现了其现代化的技术栈：

```mermaid
graph TB
subgraph "核心框架"
FASTAPI[FastAPI]
PYDANTIC[Pydantic]
UVICORN[Uvicorn]
end
subgraph "AI/ML 依赖"
OPENAI[OpenAI SDK]
ANTHROPIC[Anthropic SDK]
HTTPX[HTTPX]
end
subgraph "数据存储"
AIOSQLITE[Aiosqlite]
CHROMADB[ChromaDB]
end
subgraph "开发工具"
RUFF[Ruff]
PYTEST[Pytest]
end
subgraph "环境管理"
DOTENV[Python Dotenv]
RICH[Rich]
end
PYPROJECT[项目配置] --> FASTAPI
PYPROJECT --> PYDANTIC
PYPROJECT --> UVICORN
PYPROJECT --> OPENAI
PYPROJECT --> ANTHROPIC
PYPROJECT --> HTTPX
PYPROJECT --> AIOSQLITE
PYPROJECT --> CHROMADB
PYPROJECT --> RUFF
PYPROJECT --> PYTEST
PYPROJECT --> DOTENV
PYPROJECT --> RICH
```

**图表来源**
- [pyproject.toml:1-34](file://backend/pyproject.toml#L1-L34)

**章节来源**
- [pyproject.toml:1-34](file://backend/pyproject.toml#L1-L34)

## 性能考虑

### 并发处理策略

Kore 框架采用异步编程模型来提高性能：

1. **异步 I/O 操作**：所有网络请求和文件操作都使用异步模式
2. **连接池管理**：HTTP 客户端使用连接池减少连接开销
3. **内存缓存**：智能体状态和常用数据使用内存缓存
4. **流式处理**：支持流式响应处理大量数据

### 内存管理

```mermaid
flowchart TD
REQUEST[请求到达] --> VALIDATE[参数验证]
VALIDATE --> CACHE_CHECK{缓存命中?}
CACHE_CHECK --> |是| RETURN_CACHE[返回缓存数据]
CACHE_CHECK --> |否| PROCESS[处理请求]
PROCESS --> STORE_CACHE[存储到缓存]
STORE_CACHE --> RESPONSE[生成响应]
RETURN_CACHE --> RESPONSE
RESPONSE --> CLEANUP[清理临时资源]
CLEANUP --> END[请求结束]
```

## 故障排除指南

### 常见问题诊断

1. **工具注册失败**
   - 检查工具类是否正确继承基类
   - 验证工具名称的唯一性
   - 确认工具参数定义的完整性

2. **LLM 连接问题**
   - 验证 API 密钥配置
   - 检查网络连接状态
   - 确认模型名称拼写

3. **内存泄漏**
   - 检查异步资源的正确释放
   - 验证缓存策略的有效性
   - 监控内存使用情况

**章节来源**
- [agent_core.py](file://backend/kore/runtime/agent_core.py)
- [factory.py](file://backend/kore/llm/factory.py)

## 结论

Kore 智能体框架为自定义工具开发提供了完整的基础设施和最佳实践指导。通过理解框架的架构设计、核心组件和开发模式，开发者可以高效地创建功能强大且可靠的自定义工具。

框架的关键优势包括：
- 模块化的架构设计
- 灵活的工具系统
- 强大的 LLM 集成
- 完善的错误处理机制
- 优秀的性能表现

## 附录

### 开发环境设置

1. **Python 版本要求**：3.12+
2. **安装依赖**：使用 pip 或 conda 安装项目依赖
3. **开发工具**：Ruff 用于代码格式化，Pytest 用于测试

### 测试策略

1. **单元测试**：针对单个工具的功能测试
2. **集成测试**：测试工具与智能体的交互
3. **性能测试**：评估工具的执行效率
4. **并发测试**：验证异步处理的正确性

### 版本管理建议

1. **语义化版本控制**：遵循语义化版本规范
2. **向后兼容性**：保持 API 的向后兼容
3. **变更日志**：维护详细的版本变更记录
4. **文档同步**：及时更新相关文档